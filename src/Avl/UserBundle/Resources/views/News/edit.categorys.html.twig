{% extends "UserBundle::layout.html.twig" %}

{% block title %}
    {{ 'title.news.categorys'|trans }}
{% endblock %}

{% block content %}
    <div class="col-md-12">
        {% if currentRouteName == 'avl_news_categorys_edit' %}
            <h1>
                {{ 'title.news.categorys'|trans }}
                <small>{{ 'title.news.categorys.edit'|trans }}</small>
            </h1>
        {% else %}
            <h1>
                {{ 'title.news.categorys'|trans }}
                <small>{{ 'title.news.categorys.create'|trans }}</small>
            </h1>
        {% endif %}
        <hr>
        <div class="tabbable tabs-left">
            {% include 'UserBundle:Tabs:news.categorys.html.twig' %}
            <div class="tab-content" style="width:75%;float:left">
                <div class="tab-pane active">
                    {% include "UserBundle:News:edit_content.categorys.html.twig" %}
                </div>
            </div>
        </div>
    </div>
{% endblock content %}